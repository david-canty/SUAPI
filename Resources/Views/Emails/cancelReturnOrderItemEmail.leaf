<html>
    <body>
        <p>Hi,</p>
        <p>I would like to #(action) the following #if(count(orderItemDetails) == 1) { item } else { items } from order no #orderNo(order.id) made on #date(order.orderDate, "d MMM yyyy 'at' HH:mm").</p>
        <p><b>Item details:</b></p>
        <table>
            <thead>
                <tr>
                    <th>Item</th>
                    <th>Size</th>
                    <th>Price</th>
                    <th>Quantity to #(action)</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                #for(orderItem in orderItemDetails) {
                <tr data-item-id="#(item.id)">
                    <td>#(orderItem.item.itemName)</td>
                    <td>#(orderItem.size.sizeName)</td>
                    <td>#(orderItem.item.itemPrice)</td>
                    <td>#(orderItem.quantity)</td>
                    <td>#(orderItem.formattedTotal)</td>
                </tr>
                }
            </tbody>
        </table>
        <p><b>Item total: </b>#(formattedItemTotal)</p>
        <p><b>Payment method: </b>#(order.paymentMethod)<p>
        <p>Thank you.</p>
    </body>
</html>

